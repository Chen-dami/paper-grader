# ============================================================
#  AI-Grader 环境变量配置模板
# ============================================================
#  使用方法:
#    Windows:  set KEY=value  (当前会话)
#              setx KEY "value"  (永久，需重启终端)
#    Linux/Mac: export KEY=value
#
#  至少配置一个视觉模型 + 一个文本模型即可运行
# ============================================================

# ── 智谱 AI（推荐 · 视觉 + 文本 · 国内直连）──
# 获取 Key: https://open.bigmodel.cn/ → 个人中心 → API Keys
# 视觉模型: glm-4v-flash（推荐，¥0.5/1M input）/ glm-4v（¥5/1M）
ZHIPU_KEY=你的智谱API_KEY

# ── DeepSeek（推荐 · 纯文本 · 最便宜）──
# 获取 Key: https://platform.deepseek.com/ → API Keys
# 模型: deepseek-chat（¥1/1M）/ deepseek-reasoner（¥4/1M）
DEEPSEEK_KEY=你的DeepSeek_API_KEY

# ── OpenAI（视觉 + 文本）──
# 获取 Key: https://platform.openai.com/ → API Keys
# 模型: gpt-4o / gpt-4o-mini
# OPENAI_API_KEY=sk-你的OpenAI_API_KEY

# ── 阿里通义千问（视觉 · 国内直连）──
# 获取 Key: https://dashscope.console.aliyun.com/ → API-KEY管理
# 模型: qwen-vl-max
# BAILIAN_KEY=你的阿里API_KEY

# ── Anthropic Claude（视觉 + 文本 · 需海外网络）──
# 获取 Key: https://console.anthropic.com/ → API Keys
# 模型: claude-sonnet-4-6
# ANTHROPIC_API_KEY=sk-ant-你的Anthropic_API_KEY
